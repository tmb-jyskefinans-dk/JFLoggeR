module.exports = {
  darkMode: 'class', // enable manual dark mode via adding 'dark' class to <html>
  content: [
    './src/**/*.{html,ts}',
    './electron/**/*.ts'
  ],
  theme: {
    extend: {
      // You can extend custom colors or variables here later.
    }
  },
  plugins: []
};
